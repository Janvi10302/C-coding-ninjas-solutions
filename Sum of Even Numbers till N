#include<iostream>
using namespace std;


int main(){

    int n, sum = 0;
    cin>>n;
    
    int count = 1;
    while (count <= n)
    {
        if (count % 2 == 0)
        {
            sum = sum + count;
        }
        count++;
    }
    
    cout << sum << endl;
}
