void printIthNode(Node *head, int i) {
  // Write your code here
  int j = 0;
  Node *current = head;
  while (current != NULL) {
    if (j == i) {
      cout << current->data;
      break;
    }
    current = current->next;
    j++;
  }
}
